language: python

python:
  - 2.7

build:
  pre_ci:
    - docker pull therickys93/ubuntu14
    - docker build --rm -t therickys93/ubuntu14arduino .

  pre_ci_boot:
    image_name: therickys93/ubuntu14arduino
    image_tag: latest
    pull: false

  ci:
    - bash /arduino/test/test.sh